# API
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9

# Carga el archivo .env en local (uvicorn no lo hace solo)
python-dotenv>=1.0

# Acceso a la réplica de solo lectura de Postgres (features de los modelos)
psycopg[binary]>=3.2

# Modelos interpretables (regresión de peso, scoring)
numpy>=2.1

# Upgrade a modelos entrenados (cuando haya datos etiquetados). Descomentar:
# scikit-learn>=1.5     # abandono / adherencia (clasificación entrenada)
# xgboost>=2.1          # gradient boosting
# pandas>=2.2           # feature engineering a escala
# boto3>=1.35           # descargar imágenes del bucket S3/MinIO (visión propia)
